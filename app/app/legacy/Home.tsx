"use client";

// TEMP: if your old home used state/effects or browser APIs,
// this client component will handle it without complaining.

export default function LegacyHome() {
  return (
    <div style={{ maxWidth: 960, margin: "40px auto", padding: 16 }}>
      {/* ⬇️ Paste the JSX from your Replit home BETWEEN these markers */}
      {/* --- BEGIN REPLIT HOME JSX --- */}

      <h1>Replit Home (placeholder)</h1>
      <p>Replace this block with your Replit home JSX.</p>

      {/* --- END REPLIT HOME JSX --- */}
    </div>
  );
}